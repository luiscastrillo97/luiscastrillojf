const Icon = () => {
  return (
    <a href="/" className="transition-all hover:scale-105">
      <p className="text-xl font-semibold text-nowrap">{`<LC />`}</p>
    </a>
  );
};

export default Icon;
